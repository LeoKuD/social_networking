let initialState = {
    friends: [
        { id: 1, name: 'Dima' },
        { id: 2, name: 'Andrey' },
        { id: 3, name: 'Dima' }
    ]
}

let friendsReducer = (state = initialState) => {
    return state
}


export default friendsReducer